# Examples

Use the CLI to create a first set of predictor-agnostic manifests:

```bash
dflow-af make-manifests --out results/manifests
```

A future example will ingest structure-prediction outputs and compute stereochemical and membrane-conditioned diagnostics.
